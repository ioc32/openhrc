# templated by ansible

server:
        hide-version: yes
        ip-address: 127.0.0.1

remote-control:
        control-enable: yes
        control-interface: /var/run/nsd.sock

zone:
        name: "{{ dns.authoritative.zone }}"
        zonefile: "{{ dns.authoritative.zone }}.zone"
